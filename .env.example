# HelloFresh Google Workspace MCP Server - Environment Configuration
# Copy this file to .env and update values as needed

# =============================================================================
# REQUIRED
# =============================================================================

# Your HelloFresh Google email address
USER_GOOGLE_EMAIL=your.email@hellofresh.com

# =============================================================================
# OPTIONAL - OAuth Configuration
# =============================================================================

# OAuth credentials are embedded in the server for HelloFresh use.
# Only set these if you need to override with custom credentials:
# GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret

# Path to client_secret.json file (alternative to env vars)
# GOOGLE_CLIENT_SECRET_PATH=/path/to/client_secret.json

# =============================================================================
# OPTIONAL - Server Configuration
# =============================================================================

# Single-user mode - bypasses session mapping (useful for local development)
# MCP_SINGLE_USER_MODE=false

# HTTP server port (for streamable-http transport)
# WORKSPACE_MCP_PORT=9876

# Base URI for the server
# WORKSPACE_MCP_BASE_URI=http://localhost

# External URL (if behind a proxy)
# WORKSPACE_EXTERNAL_URL=https://your-domain.com

# =============================================================================
# OPTIONAL - OAuth 2.1 / Stateless Mode (Advanced)
# =============================================================================

# Enable OAuth 2.1 (required for FastMCP Cloud deployments)
# MCP_ENABLE_OAUTH21=false

# Stateless mode - no local credential storage
# WORKSPACE_MCP_STATELESS_MODE=false

# Allow insecure transport (HTTP instead of HTTPS) - development only!
# OAUTHLIB_INSECURE_TRANSPORT=false
